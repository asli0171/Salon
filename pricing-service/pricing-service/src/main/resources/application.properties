spring.application.name=pricing-service
server.port=8081

# JWT secret — skal matche hoved-appens secret
jwt.secret=${JWT_SECRET:salon-super-secret-jwt-key-minimum-32-chars}

# Prisregler (kan overskrives via miljøvariabler)
pricing.premium.surcharge=0.20
pricing.colour.surcharge=50.0
pricing.treatment.surcharge=30.0
pricing.long.duration.threshold=75
pricing.long.duration.surcharge=50.0
